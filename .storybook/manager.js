import { themes } from '@storybook/theming';
import { addons } from '@storybook/addons';

addons.setConfig({
  theme: themes.dark,
  brandTitle: 'Sanjeev Durge — UI components',
  brandUrl: 'https://github.com/SanjeevDurge/portfolio',
});
